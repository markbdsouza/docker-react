language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t markbdsouza/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true markbdsouza/docker-react npm run test -- --coverage
